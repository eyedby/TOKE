{
  "name": "toke-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Line-rate cryptographic validation header engine for the Δ9 Consent Stack.",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && powershell copy-item manifest.json,background.js build/",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
